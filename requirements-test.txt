# Основные зависимости для тестирования
pytest==8.3.5
pytest-asyncio==0.26.0
pytest-mock==3.14.0
pytest-cov==6.0.0
httpx==0.28.1

# Для нагрузочного тестирования
locust==2.20.0

# Дополнительные инструменты для тестирования
freezegun==1.5.1  # Для мокирования дат/времени
factory-boy==3.3.3  # Для создания тестовых данных

# Зависимости из основного проекта (дублируем, чтобы не было конфликтов)
